# 知识助手运行依赖（content_fetcher_venv，Python 3.12）
#
# 安装命令：
#   /www/content_fetcher_venv/bin/pip install -r requirements.txt
#
# 已经包含 transitive deps（rapidocr_onnxruntime 会带 onnxruntime/opencv 等）

# === Pipeline 核心 ===
httpx>=0.27           # HTTP 客户端
openai>=1.0           # DeepSeek API（OpenAI 兼容）
python-dotenv>=1.0    # .env 加载（用于 review_pusher 内部）

# === 内容抓取 ===
yt-dlp                # B站/抖音/YouTube/快手 视频元数据 + 下载
faster-whisper>=1.0   # 本地音频转录（ONNX 推理）

# === 抖音/小红书图文 ===
# XHS-Downloader 不在 PyPI，git clone 到 /www/XHS-Downloader/
# 见 install.sh 步骤
beautifulsoup4
lxml

# === OCR（图文截图）===
rapidocr_onnxruntime>=1.3   # ONNX 版 PaddleOCR，中文准确率 90%+

# === RAG（语义检索，可选）===
chromadb>=0.5              # 向量库（PersistentClient）
sentence-transformers>=3   # 拉 BAAI/bge-small-zh-v1.5 模型

# === 仪表盘（可选）===
streamlit>=1.30            # dashboard.py 用
plotly>=5                  # 图表
pandas>=2                  # 数据处理

# === 图片处理（OCR 测试图生成等）===
Pillow>=10
